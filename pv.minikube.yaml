apiVersion: v1
kind: PersistentVolume
metadata:
  name: minikube-volume
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
    - ReadOnlyMany
  hostPath:
    path: /var/minikube-volume
