apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: generic-data-static-local
spec:
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: local
