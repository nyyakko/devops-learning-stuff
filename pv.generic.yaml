apiVersion: v1
kind: PersistentVolume
metadata:
  name: generic-volume
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
    - ReadOnlyMany
  hostPath:
    path: /var/generic-volume
